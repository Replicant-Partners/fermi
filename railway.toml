[build]
builder = "nixpacks"

[build.nixpacks]
# Build the api-server binary specifically
buildCommand = "cargo build --release --bin api-server"

[deploy]
# Run the api-server binary
startCommand = "./target/release/api-server"
