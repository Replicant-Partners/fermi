[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = ""
# Temporarily disable healthcheck to allow full initialization
# healthcheckPath = "/debug/healthz"
# healthcheckTimeout = 300
restartPolicyType = "NEVER"
# Don't auto-restart - let initialization complete or fail cleanly
# After successful deployment, change back to ON_FAILURE
